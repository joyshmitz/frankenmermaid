# bv (beads viewer) local config and caches
.bv/

# macOS AppleDouble / resource fork files
._*

# Rust build artifacts
target/

# Web/WASM artifacts
pkg/
*.wasm
node_modules/
.wrangler/
dist/cloudflare-pages/

# Local scratch SQLite DBs (never tracked)
storage.sqlite3
storage.sqlite3-*
*.sqlite3.bak

# Common local artifacts (avoid accidental commits)
a.out
.plan.md
screenshots_for_diagnostics/
rustc-ice-*.txt
reports/
artifacts/
.agent-mail.yaml

# Parser test output garbage (files created by malformed diagram parsing)
B*nend*
B\[*

# Beads recovery artifacts
.beads/*.orphan_*
.beads/*.corrupt_*
.beads/*.pre_recovery_*
.beads/*.recover*.sql

# Beads history snapshots (JSONL export is tracked, history is local)
.beads/.br_history/

# Beads ephemeral DB artifacts (rebuild, recovery, test, bad snapshots)
.beads/beads.db.bad_*
.beads/beads.db.rebuild_*
.beads/beads.db.fresh_*
.beads/beads.db.cachetest*
.beads/beads.db.cyclefix*
.beads/beads.db.exporttest*
.beads/beads.db.*.dumpfix
.beads/beads.db.*.reindexed
.beads/beads.db.*.repair*
.beads/recovery_*/

# Ephemeral agent session files
SESSION_TODO.md
SESSION_REPORT.md
SESSION_*.md
verify_*.sh
newfile.txt
repro_*.rs
reproduce_*.rs

# Test output binaries/files (parser test artifacts at repo root)
/test_*

# Debug rendering output
debug_output.svg

# Audit/scan scratch files (clippy, UBS, bug scans)
clippy_*.txt
ubs_*.txt
ubs_*.jsonl
rust-bug-scan.txt
new_ubs.txt

# Lint output per-crate
layout_clippy.txt
parser_clippy.txt
term_clippy.txt
cli_clippy.txt

# Python cache dirs
__pycache__/

# Patch / diff scratch files
patch.diff

# Beads merge reject artifacts
.beads/*.rej

# Ad-hoc shell scripts (use scripts/ dir for tracked scripts)
run_test.sh

# JSONL scan pass files
pass*.jsonl

# Mojibake/garbage parser artifacts (e.g., B[🚀)
B[[]*
.env
.claude/settings.local.json

# NTM session state
.ntm/

# Per-machine IDE MCP client configs
cline.mcp.json
cursor.mcp.json
gemini.mcp.json
opencode.json
windsurf.mcp.json

# Local read-only legacy reference clone (standalone git checkout, not a submodule)
legacy_mermaid_code/
